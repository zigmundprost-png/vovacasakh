## Что проверил по противоречиям

Жёстких противоречий с тем, что мы делали раньше, нет. Структура (Hero → TrustStrip → About → … → Brief → FinalCta), цвета (синий/тёплый/красный/зелёный), форма заявки, ссылка на mnogotovarov.ru только в блоке отзывов — всё уже совпадает с промтом. Правки в основном текстовые + несколько мелких ссылок.

Замеченные нюансы (буду чинить):
- В Hero сейчас стоит метрика «700₽ ремонт мягкой мебели» — это та самая ошибка из промта.
- В блоке цен «Ремонт мягкой мебели — 700₽» — тоже надо заменить на «от 5 000 ₽».
- В Hero есть кнопка «Заполнить заявку на ремонт» (вторичная) — по промту вторичная должна быть «Посмотреть работы», а первичная — «Отправить фото или видео в MAX». Поменяю.
- В Hero визуальный блок с подписью «Мои реальные работы — добавлю фото перед запуском» — заменю.
- В Header «Заявка в MAX» / в FinalCta «Подготовить заявку» / «Сразу написать в MAX» — приведу формулировки к «Отправить фото или видео в MAX» там, где это CTA в MAX. Все эти кнопки уже ведут в MAX (через openMaxBrief или MAX_LINK), так что ссылки правильные.
- FAQ: в коде ответы есть (не «без ответов», как написано в промте). Просто перепишу формулировки точно по промту.
- Footer: текст «берусь только за то, что реально починю» заменю на формулировку из промта («…что реально могу сделать аккуратно»).

## Файлы и правки

**src/components/landing/Hero.tsx**
- H1, подзаголовок, буллеты (VALUE_PROPS), микротекст подсказки — по промту.
- TRUST_STATS: `20+ лет / работаю с мебелью`, `1500+ / сборок мебели`, `500+ / ремонтов мебели`, `700 ₽ / выезд и диагностика`. Иконку для «1500+» возьму `PackageCheck`, для «500+» — `Wrench`.
- CTA: основная — «Отправить фото или видео в MAX» (button → openMaxBrief), вторичная — «Посмотреть работы» (`<a href="#works">`). Кнопка останется видимой и на мобиле тоже (сейчас основная скрыта на мобиле — уберу `hidden sm:inline-flex`).
- Подпись под визуальным блоком: «Посмотрите реальные работы: диваны, стулья, кровати, корпусная мебель и сборка.»

**src/components/landing/Prices.tsx**
- Заменю `Ремонт мягкой мебели — 700₽` на `Ремонт мягкой мебели — от 5 000 ₽`.
- Подзаголовок и текст под ценами — по промту.

**src/components/landing/About.tsx**
- Основной текст уже совпадает — оставлю.
- Карточки HIGHLIGHTS чуть сокращу, чтобы не повторять основной текст (1–2 строки).

**src/components/landing/Faq.tsx**
- Перепишу все 7 ответов точно по тексту промта.

**src/components/landing/Brief.tsx**
- Заголовок: «Ответьте на 5 вопросов — и я быстрее пойму поломку».
- Подзаголовок: «Так лучше, чем писать "Здравствуйте, сколько стоит?"…».
- Кнопка: «Отправить Владимиру в MAX» (уже так).
- Микротекст: «Текст заявки скопируется — вставьте его в MAX и приложите фото или видео.»
- Логика отправки уже правильная (copy → open MAX_LINK), формат сообщения уже совпадает с промтом.

**src/components/landing/MaxBriefDialog.tsx**
- Заголовок модалки → «Ответьте на 5 вопросов — и я быстрее пойму поломку».
- Микротекст под кнопкой → как в промте.

**src/components/landing/FinalCta.tsx**
- Заголовок: «Покажите поломку — скажу, что делать дальше».
- Текст: по промту.
- Оставлю одну основную кнопку «Отправить фото или видео в MAX» (открывает MaxBriefDialog через openMaxBrief). Вторичные «Сразу написать в MAX» и «Позвонить» уберу, чтобы не дробить CTA — по промту в финале одна кнопка.

**src/components/landing/Footer.tsx**
- Описание: «Я частный мастер по ремонту диванов и мягкой мебели. Работаю честно: смотрю поломку, объясняю и берусь только за то, что реально могу сделать аккуратно.»
- Разделы: уже совпадают со списком из промта.

**src/components/landing/Header.tsx**
- Подпись CTA-кнопки на десктопе: «Отправить фото в MAX» вместо «Заявка в MAX» (на мобиле остаётся «MAX»). Действие то же — скролл к `#brief`/открытие диалога; оставлю существующее поведение через `#brief`.

**src/lib/links.ts** — без изменений. Все CTA уже ведут в MAX, ссылка на mnogotovarov.ru используется только в Reviews.

## Что НЕ трогаю
- Структуру секций, порядок, общий стиль.
- Блок «Отзывы» и ссылку «Открыть отзывы» на mnogotovarov.ru.
- Works, Pickup, Geography, Guarantee, ForWhom, Process, Services, TrustStrip — там тексты уже в нужном тоне и не противоречат промту.
- Цветовые токены и редизайн.

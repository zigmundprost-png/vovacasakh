## Что сделаю

В `src/components/landing/Services.tsx` добавлю отдельную карточку **«За что Владимир не берётся»** под двумя текущими карточками (Диваны / Дополнительно). Оформление — в общем стиле, но с иконками-крестиками и приглушёнными цветами (`bg-muted` / `text-muted-foreground`), чтобы блок не пугал, а спокойно информировал.

Список:
- Полная перетяжка мебели
- Сложный пошив чехлов и обивки
- Встроенная мебель на заказ
- Мелкая дешёвая сборка из коробок
- Не обещаю «починить всё» — если задача не по профилю, скажу сразу

Нижний блок «Важно знать заранее» оставлю, но переформулирую короче (перечисление переедет в новую карточку), оставив посыл: «если сомневаетесь — пришлите фото, подскажу, берусь или нет».

## Технически

- Файл: `src/components/landing/Services.tsx`
- Добавить массив `NOT_DOING` и иконку `X` из `lucide-react`
- Добавить компонент `NotDoingCard` (структура как у `ServiceCard`, но крестики вместо галочек)
- Вставить под существующей сеткой карточек, перед блоком «Важно знать заранее»
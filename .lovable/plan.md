## План правок по TextOS

Цель: убрать повторы, выровнять формулировки, без новых блоков и без переписывания стилей.

### 1. Глобальная замена географии
Везде «юг Сахалина / по югу Сахалина / на юг Сахалина» → «южная часть Сахалина».
В шапке/подвале/первом экране стоит фраза: «Южно-Сахалинск и южная часть Сахалина».

Файлы: `Hero.tsx` (строки 42, 69), `Footer.tsx` (40), `Geography.tsx`, `Pickup.tsx` (intro/перки если встречается).

### 2. Удалить блок-полосу `TrustStrip` после первого экрана
В `src/routes/index.tsx` убрать импорт и использование `<TrustStrip />` (4 карточки: Начнём с фото / Заберу и привезу / Гарантия до 1 года / Южно-Сахалинск). Файл `TrustStrip.tsx` оставить в репо, но не использовать.

### 3. Первый экран (`Hero.tsx`)
- Подзаголовок «Не спешите покупать новый» оставить как есть.
- Параграф под H1 переписать ровно по TextOS, заменив «по югу Сахалина» на «и южной части Сахалина».
- Буллеты `VALUE_PROPS` привести к точным формулировкам:
  1. Ремонтирую диваны, кресла, стулья и кровати
  2. Начинаю с фото или видео, чтобы не гонять вас и себя впустую
  3. Объясняю простым языком, что сломалось
  4. Если нужно — заберу мебель и привезу обратно
- `TRUST_STATS` — оставить текущие 4 (20+ лет / 500+ / 700 ₽ / до 1 года). «1500+ сборок» не возвращать.
- Убрать подсказку-карточку «Лучше короткое видео…» под кнопками (повтор фото/видео; та же мысль звучит в форме MAX и в финальном блоке).

### 4. Блок «Обо мне» (`About.tsx`)
- Текстовая часть: два абзаца строго по TextOS («Меня зовут Владимир…» и «Сначала смотрю поломку, объясняю…, можем договориться о заборе и возврате»). Без упоминания «фото или видео».
- Полностью удалить три карточки `HIGHLIGHTS` (Знаю мебель изнутри / Заберу и привезу / Сборка с ответственностью) — оставить только фото мастера + текст.

### 5. Чистка повторов про фото/видео
Оставить упоминание только в: первом экране (параграф под H1), первом шаге блока «Как проходит работа» (`Process.tsx` — не трогаем, там и так), форме перед MAX (`Brief.tsx` подсказка) и финальном блоке.
- `Prices.tsx`: переписать блок «Важно» без «По фото или видео»: оставить мысль, что точную цену называю после диагностики.
- `Geography.tsx`: убрать «Перед выездом лучше пришлите мне фото или видео».
- `Footer.tsx`: убрать абзац «Лучше сразу присылайте фото или короткое видео…».

### 6. География (`Geography.tsx`)
Заменить содержимое на компактный вариант:
- Заголовок секции остаётся «Где я работаю».
- Один блок с текстом: «Работаю в Южно-Сахалинске. В Корсаков, Холмск, Невельск и Долинск выезжаю по договорённости.»
- Убрать вторую карточку с фразой про фото/видео.
(Структура секции и стили — без переделок, просто упрощаем содержимое.)

### 7. Подписи к работам (`Works.tsx`)
Категории-фильтры (Все/Ремонт/Сборка) и существующий массив `WORKS` уже содержат осмысленные `title`/`desc`. Сейчас под превью показывается только бейдж категории «Ремонт/Сборка». Это и есть «общая подпись» из TextOS.
Заменим бейдж на короткий конкретный лейбл, выводимый из `WORKS[i].title` через новое поле `tag`. Для каждой работы зададим короткий `tag` из списка TextOS:
- замена пружинного блока, усиление каркаса, ремонт механизма раскладывания, замена поролона, ремонт подлокотника, устранение скрипа, ремонт стула, ремонт шкафа, сборка кровати, сборка шкафа, сборка мебели.
Соответствие проставим по смыслу title (например, work29 «пружинный блок» → «замена пружинного блока»; work35 «усиление каркаса» → «усиление каркаса»; столы/офис → «сборка мебели»; стулья → «ремонт стула»; шкафы (ремонт) → «ремонт шкафа»; шкафы (сборка) → «сборка шкафа»; кровать-сборка → «сборка кровати» и т. д.).
Бейдж категории внутри диалога остаётся (там и так есть title+desc).

### 8. Отзывы
Без изменений: 8 отзывов + «Показать ещё» уже сделаны.

### 9. Частые вопросы (`Faq.tsx`)
Переписать ответы в формулировки TextOS (короче, без «технически сомнителен»):
- Что лучше прислать: фото или видео? — «Лучше короткое видео. Снимите мебель целиком, место поломки и то, как проблема проявляется. Если видео неудобно — пришлите 2–3 фото.»
- Вы сами забираете мебель? — «Да, если ремонт неудобно делать дома. Забор, возврат, грузчиков и выезд за город обсуждаем заранее.»
- Сколько стоит выезд? — «Выезд и диагностика по Южно-Сахалинску — 700 ₽.»
- Есть ли гарантия? — «Да, до 1 года на выполненную работу при нормальной эксплуатации.»
- Делаете ли сборку мебели? — «Да, если задача подходит по объёму. Мелкую дешёвую сборку из коробок не беру.»
- Когда ремонтировать не стоит? — «Если ремонт выйдет почти как новая мебель или задача технически сомнительная, скажу об этом до начала работы.»
Вопрос «Можете назвать цену по фото?» — оставить, ответ слегка ужать.

### 10. Финальный блок (`FinalCta.tsx`)
- Текст: «Пришлите фото или короткое видео в MAX. Я посмотрю и отвечу: можно ли ремонтировать, нужен ли выезд и какой следующий шаг лучше.»
- Кнопка остаётся: «Отправить фото или видео в MAX».
- Удалить нижний абзац «Снимите мебель целиком…» (повторяет подсказку из формы).

### 11. Хедер (`Header.tsx`)
- Подсказка пункта «О мастере» — оставить «Опыт 20+ лет».
- Прочее без изменений; «40+ фото» уже стоит.

### Что НЕ трогаем
Стили, цвета, ссылку MAX, `Process.tsx`, `Services.tsx`, `Reviews.tsx`, `Pickup.tsx` (кроме географии, если встретится), `Brief.tsx`/`MaxBriefDialog.tsx` (логика и подсказка про видео остаются), `MobileStickyCta`, `DesktopFloatingCta`, `Footer` структуру.

### Технические детали
- `src/routes/index.tsx`: убрать `import { TrustStrip }` и `<TrustStrip />`.
- `Works.tsx`: расширить тип `Work` полем `tag: string`, заполнить для всех 44 элементов, заменить рендер бейджа категории на бейдж `tag` (или показать оба: категория сверху-слева, тег снизу). Мини-вариант: рендерим только `tag` снизу полупрозрачной плашкой, категория остаётся в фильтрах и в диалоге.
- Все правки — только текст/массивы, без новых зависимостей.

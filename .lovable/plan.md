## Новый блок «О Владимире»

Создаю компонент `src/components/landing/About.tsx` через `Section`:

- **eyebrow:** «Кто будет работать»
- **title:** «О Владимире»
- **intro:** три абзаца текста из брифа без изменений (от первого лица).
- **Ниже** — сетка из трёх карточек-акцентов с иконками `lucide-react` (`Hammer`, `PackageCheck`, `Users`):
  1. «Знаю мебель изнутри» — про механизмы, пружины, каркасы, фасады, ящики, фурнитуру.
  2. «Заберу и привезу» — если работу неудобно делать дома.
  3. «Сборка с ответственностью» — не беру мелочь ради галочки, на объёмах подключаю проверенных сборщиков.

Стилистика — как в существующих секциях (`bg-card`, `rounded-2xl`, `border-border`, иконка в `--card-soft` плашке). Адаптив: 1 колонка на мобиле, 2 на sm, 3 на lg.

## Размещение

`src/routes/index.tsx` — вставить `<About />` между `<Hero />` (+ `<TrustStrip />`) и `<Problem />`, чтобы знакомство шло сразу после первого экрана. Импорт добавить рядом с остальными.

## Навигация

В `src/components/landing/Header.tsx` добавить пункт `{ href: "#about", label: "О мастере" }` первым в массиве `NAV`, чтобы блок был доступен из меню.
